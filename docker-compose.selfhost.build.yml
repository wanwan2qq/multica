# Development override: build the backend/web images from the current checkout
# instead of pulling the official GHCR images.

services:
  backend:
    image: multica-backend:dev
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VERSION: ${VERSION:-dev}
        COMMIT: ${COMMIT:-unknown}
        DATE: ${DATE:-unknown}
        # Optional overrides (see docker-compose.selfhost.build.china.yml)
        GOPROXY: ${GOPROXY:-https://proxy.golang.org,direct}
        GOSUMDB: ${GOSUMDB:-sum.golang.org}

  frontend:
    image: multica-web:dev
    build:
      context: .
      dockerfile: Dockerfile.web
      args:
        NEXT_PUBLIC_APP_VERSION: dev
        COREPACK_NPM_REGISTRY: ${COREPACK_NPM_REGISTRY:-https://registry.npmjs.org}
        npm_config_registry: ${NPM_CONFIG_REGISTRY:-https://registry.npmjs.org}
        # Set SHARP_WASM=1 on x86-64-v1 / old KVM CPUs that fail next build with
        # "Prebuilt binaries for Linux x64 require v2 microarchitecture"
        # (pins sharp@0.32.6; flag name kept for existing .env files).
        SHARP_WASM: ${SHARP_WASM:-0}
